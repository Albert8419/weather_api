// Defining an interface for AQI data
interface AQIData {
  city: string;          // Represents the name of the city
  qualityIndex: number;  // Represents the air quality index
  pollutant: string;     // Represents the dominant pollutant
}
